export abstract class ArticleRepository {
  // Definir les requetes comme suit:

  // abstract getAllArticles(): Observable<ArticleEssentialDto[]>;
  // abstract getArticleById(id: string): Observable<ArticleDto>;
  // abstract createArticle(article: CreerArticleCommandDto): Observable<void>;
  // abstract updateArticle(article: ModifierArticleCommandDto): Observable<void>;
  // abstract deleteArticle(id: string): Observable<void>;
}
